<% breadcrumb.add "#{t('b.from')} #{@redirect.source} #{t('b.to')} #{@redirect.destination}" %>
<% breadcrumb.add t('b.edit'), '#' %>
<%= render 'backend/breadcrumbs' %>
<%= render 'backend/redirects/form', model: @redirect %>
