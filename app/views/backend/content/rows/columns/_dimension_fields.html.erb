<% Udongo::Configs::FlexibleContent::BREAKPOINTS.each do |bp| %>
  <%=
    form.input(
      "width_#{bp}",
      as: :select,
      collection: options_for_column_widths,
      include_blank: false
    )
  %>
<% end %>
