machine:
  ruby:
    version: 2.2.2

dependencies:
  pre:
    - cp config/secrets.dist.yml config/secrets.yml
    - mv circle.database.yml spec/dummy/config/database.yml

general:
  build_dir: spec/dummy
