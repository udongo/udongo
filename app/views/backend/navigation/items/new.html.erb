<!-- TODO breadcrumb -->

<%= simple_form_for(@model, url: backend_navigation_items_path) do |f| %>
  <%= f.input :page_id, collection: ::Page.order(:description).map { |p| [p.description, p.id] }, include_blank: true %>
  <%= render 'backend/form_actions', cancel_url: backend_navigations_path %>
<% end %>
